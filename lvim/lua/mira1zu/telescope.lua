-- lvim.builtin.telescope.defaults = {
--     unpack(lvim.builtin.telescope.defaults),
--     -- or use vim.list_extend()
-- }

lvim.builtin.telescope.theme = "center"

-- lvim.builtin.project.active = false            -- turn off default project viewer

-- require("telescope").load_extension("project") -- keybinds are defined in which_key
